export default function ChatbotTestPage() {
  return (
    <main className="min-h-screen flex flex-col items-center justify-center text-white bg-gray-900">
      <h1 className="text-4xl font-bold mb-4">Chatbot Test Page</h1>
      <p className="text-lg">
        If the chatbot is configured correctly, the icon should appear in the
        bottom right corner.
      </p>
    </main>
  );
}
